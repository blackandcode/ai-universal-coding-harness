# Security

Please report security issues privately to the repository maintainer rather than opening a public issue with exploit details.

The orchestrator executes AI-directed development operations in local repositories. Review `docs/permissions.md` before using permissive modes. The default `auto_safe` mode intentionally escalates uncertain operations to the configured reviewer.

The tool never automatically pushes or merges AI branches.
