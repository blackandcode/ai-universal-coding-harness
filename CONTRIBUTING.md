# Contributing

1. Create a focused branch.
2. Read `AGENTS.md` and the relevant architecture docs.
3. Keep harness-specific behavior inside harness adapters.
4. Add tests for behavior changes.
5. Run `npm run verify`.
6. Update `CHANGELOG.md` for user-visible changes.

Pull requests should describe the problem, architectural impact, tests performed, and any migration/configuration changes.
